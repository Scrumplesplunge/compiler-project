DEF n = 2, size = 1 + (n * (1 + n)) :
CHAN c[size] :
VAR x[size] :

ALT
  1 = 1 & c[0] ? x[0]
    print.dec(0)
  ALT i = [1 FOR n]
    2 = 2 & c[i] ? x[i]
      print.dec(i)
    ALT j = [1 FOR n]
      3 = 3 & c[(n * i) + j] ? x[(n * i) + j]
        print.dec((n * i) + j)
